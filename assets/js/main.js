async function loadSamplePlanets(){const holders=document.querySelectorAll("[data-planets]");if(!holders.length)return;const res=await fetch("data/sample-planets.json");const planets=await res.json();holders.forEach(holder=>{const limit=Number(holder.getAttribute("data-limit")||planets.length);holder.innerHTML=planets.slice(0,limit).map(p=>`<article class="card feature"><img src="assets/images/planet-${p.token_id}.svg" alt="${p.name} planet artwork"><div class="card-body"><p class="kicker">${p.tier} · Token #${p.token_id}</p><h3>${p.name}</h3><p>${p.archetype||"Exoplanet"} · ${p.star_system||"Unknown system"} · ${p.discovery_method||"Unknown method"}</p><p class="small">Moons: ${p.moons||"0"} · Rings: ${p.rings||"None"} · Discovery year: ${p.discovery_year||"Unknown"}</p></div></article>`).join("");});}loadSamplePlanets();
